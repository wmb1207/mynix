#!/bin/sh

# Set wallpaper
feh --bg-scale /path/to/your/wallpaper.jpg

# Start compositor
picom &

# Launch sxhkd for keybindings
sxhkd &

# Launch dmenu with custom styling (black background)
dmenu_run -nb '#000000' -nf '#ffffff' -sb '#444444' -sf '#ffffff' &

# Start bspwm
exec bspwm
