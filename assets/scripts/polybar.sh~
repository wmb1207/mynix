#!/usr/bin/env bash

pkill -9 polybar

DISPLAY=$1

case "$DISPLAY" in
    laptop)
	polybar -c ~/.config/polybar/config.ini top &
	;;
    *)
	polybar -c ~/.config/polybar/config.ini top-external &
	polybar -c ~/.config/polybar/config.ini top &
	;;
esac
